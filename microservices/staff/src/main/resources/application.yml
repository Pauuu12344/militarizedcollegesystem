spring:
  application:
    name: staff

  datasource:
    url: jdbc:mysql://localhost:3306/mcs-staff?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  main:
    allow-bean-definition-overriding: true

server:
  port: 8084
